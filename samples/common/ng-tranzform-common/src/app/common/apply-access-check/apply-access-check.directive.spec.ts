import { ApplyAccessCheckDirective } from './apply-access-check.directive';

xdescribe('ApplyAccessCheckDirective', () => {
  it('should create an instance', () => {
    /*
    const directive = new ApplyAccessCheckDirective();
    expect(directive).toBeTruthy();
    */
  });
});
