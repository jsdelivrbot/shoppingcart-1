import { ResponsiveColumnDirective } from './responsive-column.directive';

describe('ResponsiveColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponsiveColumnDirective();
    expect(directive).toBeTruthy();
  });
});
