import { YearToAgePipe } from './year-to-age.pipe';

describe('YearToAgePipe', () => {
  it('create an instance', () => {
    const pipe = new YearToAgePipe();
    expect(pipe).toBeTruthy();
  });
});
