export interface SaveMessageReply {
    /**
     * tenantEnrollmentId of the user to whom the reply need to be send.
     */
    tenantEnrollmentId?: string;
    /**
     * reply for message having messageId
     */
    messageId?: string;
    /**
     * conversationId in the converasation
     */
    conversationId?: string;
    /**
     * messageBody in the converasation
     */
    messageBody?: string;
}
