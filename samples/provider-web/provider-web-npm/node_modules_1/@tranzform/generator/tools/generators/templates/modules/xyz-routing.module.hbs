    import { NgModule }             from '@angular/core';
    import { RouterModule, Routes } from '@angular/router';
    import { AuthGuard } from './../shared/guards/authguard.service';
    import { LayoutComponent } from './../layout/layout/layout.component';
    import { {{pascalCase componentname}}Component } from './{{camelCase componentname}}/{{camelCase componentname}}.component';
    /** Generator: End of imports */
    
    const {{camelCase componentname}}Routes: Routes = [
      { path: '',  component: LayoutComponent, canActivate: [AuthGuard],
        children:[ 
        { path: '{{routename}}',  component: {{pascalCase componentname}}Component,pathMatch:'full' }
        /** Generator:End of Routes */
        ]
      }
    ];
    @NgModule({
      imports: [
        RouterModule.forChild({{camelCase componentname}}Routes)
      ],
      exports: [
        RouterModule
      ]
    })
    export class {{pascalCase name}}RoutingModule { }