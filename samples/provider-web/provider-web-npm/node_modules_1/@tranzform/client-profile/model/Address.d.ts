export interface Address {
    /**
     * The type of address.
     */
    addressType?: Address.AddressTypeEnum;
    /**
     * The type of address.
     */
    preferredType?: Address.PreferredTypeEnum;
    /**
     * Address line 1.
     */
    address1?: string;
    /**
     * Address line 2.
     */
    address2?: string;
    /**
     * Address line 3.
     */
    address3?: string;
    /**
     * The city associated with the member address.
     */
    city?: string;
    /**
     * If the country is USA, then provide the 2 letter state code. For all other countries, provide the state name.
     */
    state?: string;
    /**
     * If the country is USA, then provide the minimum 5 digit zip code or the formatted 5 digit.  For all other countries, provide a zip code, based on the country.
     */
    zipCode?: string;
    /**
     * Name of the county associated with the address. This attribute is only applicable to US addresses.
     */
    county?: string;
    /**
     * Name of the country associated with the address.
     */
    country?: string;
}
export declare namespace Address {
    enum AddressTypeEnum {
        Home,
        Work,
        Mail,
        Alternate,
        Confidential,
    }
    enum PreferredTypeEnum {
        Primary,
        Secondary,
    }
}
