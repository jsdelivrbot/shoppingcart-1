export interface SubscriberSubGroupAttributeDetails {
    /**
     * The name of the subgroup to which the subscriber is assigned.
     */
    subGroupName?: string;
    /**
     * groupId to which the subscriber is assigned.
     */
    groupId?: string;
    /**
     * The name of the subgroup to which the subscriber is assigned.
     */
    subGroupId?: string;
    /**
     * The code of the sub group assigned to subscriber.
     */
    packageCode?: string;
}
