export interface MasterDataValidate {
    /**
     * Category for which validation needs to be performed
     */
    category?: string;
    /**
     * Language code. For example, \"en\" or \"es\".
     */
    language?: string;
    /**
     * MasterData code with which validation needs to be performed.
     */
    code?: string;
}
