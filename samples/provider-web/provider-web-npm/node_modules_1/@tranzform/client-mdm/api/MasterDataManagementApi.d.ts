import { Http, Headers } from '@angular/http';
import { Response } from '@angular/http';
import { Observable } from 'rxjs/Observable';
import 'rxjs/add/operator/map';
import * as models from '../model/models';
import { Configuration } from '../configuration';
export interface IgetCategoriesParams {
    /** Provide the single category or multiple categories(comma seperated). */
    category: string;
}
export interface IvalidateMasterDataParams {
    /**  */
    body: Array<models.MasterDataValidate>;
}
export declare class MasterDataManagementApi {
    protected http: Http;
    protected basePath: string;
    defaultHeaders: Headers;
    configuration: Configuration;
    constructor(http: Http, basePath: string, configuration: Configuration);
    /**
    * Get Master Data Details
    * Get the list of master data details based on provided multiple categories and also based on language preference either en or es.
    * @param params Parameters.
    */
    getCategories(params: IgetCategoriesParams, extraHttpRequestParams?: any): Observable<Array<models.MasterData>>;
    /**
    * Validate Category Codes
    * Validate the code provided for each category.
    * @param params Parameters.
    */
    validateMasterData(params: IvalidateMasterDataParams, extraHttpRequestParams?: any): Observable<Array<models.MasterDataValidateVO>>;
    /**
     * Get Master Data Details
     * Get the list of master data details based on provided multiple categories and also based on language preference either en or es.
     * @param params.category Provide the single category or multiple categories(comma seperated).
     */
    getCategoriesWithHttpInfo(params: IgetCategoriesParams, extraHttpRequestParams?: any): Observable<Response>;
    /**
     * Validate Category Codes
     * Validate the code provided for each category.
     * @param params.body
     */
    validateMasterDataWithHttpInfo(params: IvalidateMasterDataParams, extraHttpRequestParams?: any): Observable<Response>;
}
