import { Observable } from 'rxjs/Observable';
import 'rxjs/add/operator/first';
import { TranslateService } from '@ngx-translate/core';
import { WebAuthenticationApi, UserInfoVO } from '@tranzform/client-auth';
/**
 * Load and cache user info.
 */
export declare class UserService {
    private _user;
    private readySubject;
    constructor(webAuthApi: WebAuthenticationApi, translate: TranslateService);
    readonly user: UserInfoVO;
    /**
     * An observable that will notify when user info is available.
     */
    readonly ready: Observable<UserInfoVO>;
}
