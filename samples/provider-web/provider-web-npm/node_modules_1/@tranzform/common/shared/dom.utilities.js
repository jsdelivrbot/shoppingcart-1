var DomUtilities = (function () {
    function DomUtilities() {
    }
    DomUtilities.getAbsoluteOffset = function (element) {
        var offset = {
            left: 0,
            top: 0,
        };
        while (element) {
            offset.left += element.offsetLeft;
            offset.top += element.offsetTop;
            element = element.offsetParent;
        }
        return offset;
    };
    DomUtilities.scrollTo = function (element) {
        window.scroll(0, DomUtilities.getAbsoluteOffset(element).top - 40);
    };
    return DomUtilities;
}());
export { DomUtilities };
//# sourceMappingURL=dom.utilities.js.map