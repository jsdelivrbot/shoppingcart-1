import { Injectable } from '@angular/core';
import { SVGCacheService } from 'ng-inline-svg';
var SvgService = (function () {
    function SvgService(svgCache) {
        var baseTags = document.getElementsByTagName('base');
        if (baseTags.length) {
            svgCache.setBaseUrl({
                baseUrl: baseTags[0].getAttribute('href')
            });
        }
    }
    SvgService.decorators = [
        { type: Injectable },
    ];
    /** @nocollapse */
    SvgService.ctorParameters = function () { return [
        { type: SVGCacheService, },
    ]; };
    return SvgService;
}());
export { SvgService };
//# sourceMappingURL=svg.service.js.map