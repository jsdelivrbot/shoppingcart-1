import { Http } from '@angular/http';
import { Observable } from 'rxjs/Observable';
import 'rxjs/add/operator/catch';
import 'rxjs/add/operator/map';
import 'rxjs/add/operator/do';
import { RoleBasedAccessControlApi } from '@tranzform/client-authorization';
export declare class AccessCheckService {
    private rolesService;
    private http;
    private capabilitiesObservable;
    private cachedData;
    constructor(rolesService: RoleBasedAccessControlApi, http: Http);
    initPermissions(): void;
    getAllPermissions(): Observable<RoleBasedAccessControlApi[]>;
    getCapabilities(capabilityKey: string): Array<any>;
    getCapabilitesCached(): Observable<any>;
}
