var ObjectUtilities = (function () {
    function ObjectUtilities() {
    }
    /**
     * A very basic equals implementation. This only checks shallow values of each object and expects them
     * to have the same schema.
     */
    ObjectUtilities.equals = function (obj1, obj2) {
        if (obj1 === obj2) {
            return true;
        }
        if (obj1 instanceof Object && obj2 instanceof Object) {
            var keyCount = Object.keys(obj2).length;
            for (var p in obj1) {
                if (obj1[p] !== obj2[p]) {
                    return false;
                }
                keyCount--;
            }
            return keyCount === 0;
        }
    };
    return ObjectUtilities;
}());
export { ObjectUtilities };
//# sourceMappingURL=object.utilities.js.map