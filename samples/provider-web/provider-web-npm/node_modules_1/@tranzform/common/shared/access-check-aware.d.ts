export interface AccessCheckAware {
    applyAccessControl(Capabilities: any): any;
}
export declare class AccessCheckProvider implements AccessCheckAware {
    capabilites: Capabilities;
    applyAccessControl(capabilites: Capabilities): void;
}
export interface Capabilities {
    read?: boolean;
    write?: boolean;
    update?: boolean;
    delete?: boolean;
}
