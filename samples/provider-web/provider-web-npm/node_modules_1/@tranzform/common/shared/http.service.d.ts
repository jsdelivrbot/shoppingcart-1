import { Http, RequestOptionsArgs, Request, Response, RequestOptions, XHRBackend } from '@angular/http';
import { ConfigModule } from './config.constant';
import { Observable } from 'rxjs/Observable';
import 'rxjs/add/observable/throw';
import 'rxjs/add/operator/map';
import 'rxjs/add/operator/catch';
export declare function HttpServiceFactory(backend: XHRBackend, options: RequestOptions): HttpService;
export declare class HttpService extends Http {
    appConfig: ConfigModule;
    private authService;
    private getHeaders(existingHeaders?);
    request(url: string | Request, options?: RequestOptionsArgs): Observable<Response>;
    private catchAuthError(self);
}
