import { Injectable } from '@angular/core';
/**
 * Provides a way to interact with the common shell.
 */
var ShellService = (function () {
    function ShellService() {
    }
    Object.defineProperty(ShellService.prototype, "messageCount", {
        /**
         * The number of messages to display in the header.
         */
        set: function (count) {
            this._messageCount = !count ? undefined
                : count > ShellService.MAX_MESSAGE_COUNT ? ShellService.MAX_MESSAGE_COUNT + '+'
                    : count;
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(ShellService.prototype, "notificationCount", {
        /**
         * The number of notifications to display in the header.
         */
        set: function (count) {
            this._notificationCount = !count ? undefined
                : count > ShellService.MAX_NOTIFICATION_COUNT ? ShellService.MAX_NOTIFICATION_COUNT + '+'
                    : count;
        },
        enumerable: true,
        configurable: true
    });
    ShellService.MAX_MESSAGE_COUNT = 9;
    ShellService.MAX_NOTIFICATION_COUNT = 19;
    ShellService.decorators = [
        { type: Injectable },
    ];
    /** @nocollapse */
    ShellService.ctorParameters = function () { return []; };
    return ShellService;
}());
export { ShellService };
//# sourceMappingURL=shell.service.js.map