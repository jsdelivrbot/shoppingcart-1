import { Router } from '@angular/router';
import 'rxjs/add/operator/filter';
import { TranslateService } from '@ngx-translate/core';
export declare class HeaderAndCrumbsComponent {
    private translate;
    simpleCrumb: boolean;
    private appTitle;
    routeTitle: string;
    singleCrumb: any;
    private singleCrumbParams;
    crumbs: any[];
    private crumbParams;
    private titleParams;
    /**
     * Update page title, header, and breadcrumbs when route is activated.
     */
    constructor(router: Router, translate: TranslateService);
}
