import { ElementRef, Renderer } from '@angular/core';
import { AccessCheckService } from '../shared/access-check.service';
import { AccessCheckProvider } from '../shared/access-check-aware';
export declare class ApplyAccessCheckDirective {
    private parent;
    private renderer;
    private items;
    private currentCapability;
    capability: string[];
    constructor(parent: AccessCheckProvider, el: ElementRef, renderer: Renderer, accessCheckService: AccessCheckService);
    private applyCapabilityRules(el, accessCheckService);
    private setDisplayNone(control);
    private removeControl(control);
    private removeDisabledAttribute(control);
    private addDisabledAttribute(control);
}
