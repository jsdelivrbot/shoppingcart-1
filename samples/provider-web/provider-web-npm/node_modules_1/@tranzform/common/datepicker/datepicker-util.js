var DatepickerUtil = (function () {
    function DatepickerUtil() {
    }
    DatepickerUtil.toNgbDate = function (date) {
        return date && {
            year: date.getFullYear(),
            month: date.getMonth() + 1,
            day: date.getDate(),
        };
    };
    DatepickerUtil.toDate = function (ngbDate) {
        return ngbDate && new Date(ngbDate.year, ngbDate.month - 1, ngbDate.day);
    };
    DatepickerUtil.compare = function (date1, date2) {
        if (date1.year > date2.year) {
            return 1;
        }
        if (date1.year < date2.year) {
            return -1;
        }
        if (date1.month > date2.month) {
            return 1;
        }
        if (date1.month < date2.month) {
            return -1;
        }
        return date1.day - date2.day;
    };
    return DatepickerUtil;
}());
export { DatepickerUtil };
//# sourceMappingURL=datepicker-util.js.map